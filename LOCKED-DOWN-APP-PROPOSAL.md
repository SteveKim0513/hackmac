# 기획: 잠금형 배포 앱 (가칭)

이 문서는 2026-09-09 대화에서 나온 아이디어를 정리한 기획 메모다. 아직 실행 계획이 아니라 개념 정리 단계.

## 아이디어

지금 mac-shortcut-manager와 백엔드 구조(스크립트 기반 단축키 실행)는 동일하지만, 사용자 경험은 완전히 다른 앱을 만든다.

- 개발자(나)가 기능을 기획하고 단축키를 할당해서 스크립트로 구현.
- 패키징해서 배포.
- 사용자는 다운로드만 하면 정해진 기능을 그대로 쓴다 — 추가/수정/삭제 UI 없음.
- 앱을 열면 아무 설정 화면도 없고 인사말 정도만 보여준다.

즉 지금 프로젝트가 "사용자가 직접 단축키를 만드는 도구"라면, 이건 "내가 이미 만들어놓은 기능만 동작하는 완성형 제품"이다.

## 실현 가능성

기술적으로 가능하며, 이 프로젝트의 엔진(`electron/registry.ts`, `hotkeys.ts`, `runner.ts`, `parser.ts`, `shared/types.ts`)을 거의 그대로 재사용할 수 있다.

바뀌는 부분:
- 스크립트 소스: 사용자가 편집 가능한 `scripts/` 폴더 → 앱 번들 안 읽기 전용 리소스(electron-builder `extraResources`)로 대체.
- UI: `src/manager/`(관리 화면), `src/palette/`(팔레트), `?palette=1` 분기 등 편집·조회 UI를 전부 제거하고 정적 인사말 화면 하나로 대체.
- 유지: 앱 시작 시 번들 스크립트를 파싱해 전역 단축키를 등록하는 흐름, `HotkeyRegistrar`의 충돌 감지 로직, `runner.ts`의 실행 로직.

폴더 워처(현재 `registry.ts`의 핵심 동기화 루프)는 스크립트가 런타임에 바뀌지 않으므로 새 앱에서는 불필요해질 가능성이 크다 — 시작 시 1회 파싱으로 충분.

## 은닉 수준에 대한 판단

"사용자가 못 보게"에는 두 단계가 있고, 이 둘을 구분해서 목표를 정해야 한다.

| 목표 | 방법 | 비고 |
|---|---|---|
| 사용자가 못 바꾸게 | UI 자체를 안 줌 + 스크립트를 읽기 전용 리소스로 번들 | 지금 엔진 그대로 충분 |
| 사용자가 쉽게 못 읽게 | 오케스트레이션을 컴파일 언어로, 민감 문자열은 최소 난독화 | 문턱만 높임, 완전 차단 아님 |
| 절대 못 읽게 | 사실상 불가능 | 로컬 실행 앱은 리버스 엔지니어링에서 자유로울 수 없음 |

이 앱은 결국 `osascript`/AppleScript, 셸 명령을 문자열로 실행하는 구조라, 바이너리로 컴파일해도 그 안의 명령 문자열은 `strings` 한 방으로 대부분 드러난다. macOS 앱 번들도 "패키지 내용 보기"로 열리는 구조라 완전한 블랙박스는 원천적으로 불가능. 따라서 **"바꾸지 못하게"를 1차 목표로 잡고, "읽지 못하게"는 필요성이 명확해질 때 별도로 검토**하는 게 합리적이다. 참고로 script-only 원칙([PRINCIPLES.md](PRINCIPLES.md))과 완전 은닉은 근본적으로 충돌한다.

## 프로젝트 분리 방안

이 앱은 별도 제품(다른 사용자층, 다른 배포 주기, 다른 브랜딩)이므로 이 레포 안에 폴더 하나 추가하는 방식보다 **별도 git 레포**를 추천.

이유:
- 이 레포의 릴리즈 자동화(`npm run release`, `scripts/cleanup-releases.sh`, `SteveKim0513/mac-shortcut-manager`에 스코프된 GH_TOKEN)가 이 레포 하나에 고정 배선되어 있어, 다른 번들 ID·아이콘·버전 체계를 가진 앱을 같이 넣으면 자동화가 꼬인다.
- 두 앱의 엔진은 시간이 지나며 갈라질 가능성이 크다(배포용 앱은 폴더 워처·핫리로드가 불필요해질 수 있음). 처음부터 분리해두는 편이 나중에 떼어내는 것보다 싸다.

## 다음 단계 (실행 전)

1. 이 프로젝트에 엔진 설계 문서(가칭 ARCHITECTURE.md) 작성 — registry/hotkeys/runner/parser 연동 방식, ".sh 헤더가 진실의 원천" 원칙, CLAUDE.md에 쌓인 환경 특이사항 중 새 레포로 이식 가능한 교훈 정리.
2. 위 문서를 참고자료 삼아 새 레포 부트스트랩(엔진 코드 복사, UI는 인사말 화면만).
3. 새 레포의 배포/서명/공증/GitHub Releases 설정은 이 레포 패턴을 복제하되, 계정·레포 이름은 새로 결정.

아직 확정된 실행 계획은 아니며, 다음 대화에서 우선순위와 착수 시점을 정한다.
